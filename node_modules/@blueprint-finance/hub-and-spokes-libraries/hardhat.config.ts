import { HardhatUserConfig } from "hardhat/config";
import "@nomiclabs/hardhat-ethers"; // Add other plugins if needed

const config: HardhatUserConfig = {
  solidity: "0.8.24", // Specify your Solidity version here
  paths: {
    sources: "./src", // Use `src` directory for Solidity contracts
    tests: "./test",
    cache: "./cache",
    artifacts: "./artifacts"
  }
};

export default config;